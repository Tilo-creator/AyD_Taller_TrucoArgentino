<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inicio del Juego</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
    body {
        font-family: 'Arial', sans-serif;
        background-color: #28a745; /* Fondo verde */
        background-image: url('https://example.com/cartas-y-fichas.jpg'); /* URL de una imagen de fondo con cartas y fichas */
        background-size: cover;
        background-position: center;
        color: #fff;
        margin: 0;
        padding: 0;
        display: flex;
        align-items: center; /* Centrar verticalmente */
        justify-content: center; /* Centrar horizontalmente */
        height: 100vh; /* Altura completa de la pantalla */
        text-align: center; /* Centrar el texto */
        flex-direction: column; /* Para apilar el título y el contenedor verticalmente */
    }

    .header {
        margin-bottom: 20px;
        background-color: rgba(0, 0, 0, 0.7); /* Fondo semitransparente */
        padding: 10px 20px;
        border-radius: 10px;
    }
    
    .container {
        background-color: rgba(0, 0, 0, 0.7); /* Fondo semitransparente */
        padding: 40px;
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.5); /* Sombra para mayor contraste */
    }
    
    h1, h2 {
        text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5); /* Sombra para el texto */
    }
    
    h1 {
        font-size: 4rem;
        color: #ffffff; /* Color blanco */
        margin-bottom: 20px;
        transition: all 0.2s ease-in-out;
    }

    h1:hover {
        animation: vibrate 0.3s linear infinite;
    }

    @keyframes vibrate {
        0% { transform: translate(0); }
        25% { transform: translate(2px, 2px); }
        50% { transform: translate(0); }
        75% { transform: translate(-2px, -2px); }
        100% { transform: translate(0); }
    }

    h2 {
        font-size: 3rem;
        color: #ffd700; /* Color amarillo */
    }
    
    .btn {
        display: inline-block;
        padding: 15px 30px; /* Botones más grandes */
        font-size: 1.5rem; /* Texto más grande */
        text-decoration: none;
        color: #fff;
        background-color: #007bff;
        border: none;
        border-radius: 50px; /* Bordes más redondeados */
        cursor: pointer;
        transition: background-color 0.3s ease, transform 0.3s ease; /* Añadir transición para suavizar */
        margin: 10px; /* Espacio entre botones */
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3); /* Sombra para botones */
    }
    
    .btn:hover {
        background-color: #0056b3;
        transform: scale(1.1); /* Agrandar un poco al pasar el mouse */
    }
    
    .btn-primary {
        background-color: #007bff;
    }
    
    .btn-secondary {
        background-color: #6c757d;
    }
    
    </style>
</head>
<body>
    <div class="header">
        <h2>Juegos de Cartas</h2>
    </div>
    <div class="container">
        <h1 class="display-2">Bienvenidos</h1>
        <a href="/login" class="btn btn-primary btn-lg">Iniciar sesión</a>
        <a href="/registro" class="btn btn-secondary btn-lg">Registrarse</a>
    </div>
</body>
</html>
