<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pregunta</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <h1>Pregunta:</h1>
    <p><%= @question.description %></p>
    
    <h2>Opciones:</h2>
    <ul>
        <% @question.options.split(',').each do |option| %>
            <li>
                <form action="/preguntas/responder" method="post" style="display:inline;">
                    <input type="hidden" name="pregunta_id" value="<%= @question.id %>">
                    <input type="hidden" name="respuesta" value="<%= option.strip %>">
                    <button type="submit"><%= option.strip %></button>
                </form>
            </li>
        <% end %>
    </ul>

    <% if @resultado %>
        <div id="mensaje">
            <p><%= @resultado %></p>
        </div>
        <script>
            // Ocultar el mensaje después de 5 segundos y recargar la página
            setTimeout(function() {
                window.location.href = '/preguntas';
            }, 5000);
        </script>
    <% end %>
</body>
</html>
