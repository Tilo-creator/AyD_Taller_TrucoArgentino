<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Truco Argentino</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap');

        body {
            font-family: 'Arial', sans-serif;
            background-image: url('/TRUCO.jpg'); /* URL del fondo de pantalla */
            background-size: cover;
            background-position: center center;
            background-repeat: no-repeat;
            min-height: 100vh;
            color: #fff;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            margin: 0;
            padding: 0;
        }
        .titulo {
            font-size: 3rem;
            margin-bottom: 20px;
            font-weight: bold;
            color: #ffeb3b; /* Cambiado a amarillo dorado */
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
            font-family: 'Press Start 2P', cursive;
        }
        .submenu {
            display: flex;
            flex-wrap: wrap;
            justify-content: center; /* Centrando los botones */
            gap: 15px; /* Espacio entre botones */
        }
        .submenu a, .submenu button {
            text-decoration: none;
            color: #fff;
            margin: 10px;
            font-size: 1.5rem;
            padding: 10px 20px;
            border-radius: 10px;
            transition: transform 0.3s ease, box-shadow 0.3s ease, background-color 0.3s ease;
            display: inline-flex;
            align-items: center;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
            border: none;
        }
        .btn-rojo {
            background-color: #d32f2f;
            border-color: #d32f2f;
        }
        .btn-azul {
            background-color: #1976d2;
            border-color: #1976d2;
        }
        .btn-verde {
            background-color: #388e3c;
            border-color: #388e3c;
        }
        .btn-amarillo {
            background-color: #ffeb3b;
            border-color: #ffeb3b;
            color: #000;
        }
        .btn-negro {
            background-color: #000; /* Color negro para el botón */
            border-color: #000;
            color: #fff; /* Texto blanco */
        }
        .submenu a:hover, .submenu button:hover {
            transform: translateY(-5px); /* Elevar el botón */
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.6); /* Sombra más fuerte */
        }
        .btn-rojo:hover {
            background-color: #a32727; /* Oscurecer rojo */
        }
        .btn-azul:hover {
            background-color: #155a9c; /* Oscurecer azul */
        }
        .btn-verde:hover {
            background-color: #2a662b; /* Oscurecer verde */
        }
        .btn-amarillo:hover {
            background-color: #d4b300; /* Oscurecer amarillo */
        }
        .btn-negro:hover {
            background-color: #333; /* Oscurecer negro */
        }
        .submenu i {
            margin-right: 10px; /* Espacio entre el icono y el texto */
        }
        .hearts {
            display: flex;
            position: absolute; /* Cambia a posición absoluta */
            top: 20px; /* Ajusta el espacio desde el top */
            right: 20px; /* Ajusta el espacio desde la derecha */
        }

        .heart {
            font-size: 50px;
            color: red;
            margin-right: 10px;
        }

        .heart.empty {
            color: lightgray;
        }
    </style>
</head>
<body>
    <div class="container text-center">
        <!-- Aquí se mostrarán los corazones -->
        <div class="hearts">
            <% (1..3).each do |i| %>
                <% if i <= vida.cantidadDeVidas %>
                    <i class="fas fa-heart heart"></i>
                <% else %>
                    <i class="fas fa-heart heart empty"></i>
                <% end %>
            <% end %>
        </div>

        <div class="titulo">Truco Argentino</div>

        <div class="submenu mb-3">
            <a href="/lecciones" class="btn btn-rojo"><i class="bi bi-book"></i>Tomar Lecciones</a>
            <a href="/preguntas" class="btn btn-azul"><i class="bi bi-question-circle"></i>Responder Preguntas</a>
            <a href="/estadisticas" class="btn btn-amarillo"><i class="bi bi-bar-chart"></i>Ver Estadísticas</a>
            <a href="/juegos" class="btn btn-verde"><i class="bi bi-arrow-left"></i>Volver</a>
            <a href="/generarPregunta" class="btn btn-negro"><i class="bi bi-book"></i>Generar Pregunta</a>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/js/all.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js" integrity="sha384-I7E8VVD/ismYTF4hNIPjVp/Zjvgyol6VFvRkX/vR+Vc4jQkC+hVqc2pM8ODewa9r" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js" integrity="sha384-0pUGZvbkm6XF6gxjEnlmuGrJXVbNuzT9qBBavbLwCsOGabYfZo0T0to5eqruptLy" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.js"></script>

</body>
</html>
