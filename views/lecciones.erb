<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lecciones</title>
    <link rel="stylesheet" href="../css/lecciones.css"> <!-- Ruta relativa corregida -->

    <script>
        // Función para manejar el clic en cada lección
        function toggleLesson(lesson) {
            lesson.classList.toggle('active');
        }
    </script>
</head>
<body>
    <div class="container">
        <h1>Lecciones Disponibles</h1>
        <% @lecciones.each do |leccion| %>
            <div class="lesson" onclick="toggleLesson(this)">
                <h2><%= leccion.title %></h2>
                <p><strong>Contenido:</strong> <%= leccion.chapter %></p>
                <p><strong>Descripción:</strong> <%= leccion.description %></p>
            </div>
        <% end %>
    </div>
    <a href="javascript:history.back()" class="back-button">Volver</a>
</body>
</html>
